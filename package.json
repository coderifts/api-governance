{
  "name": "api-governance",
  "private": true,
  "description": "CodeRifts API governance distribution: Claude plugin, OpenAI/Codex package, Copilot kit, Cursor plugin",
  "license": "MIT",
  "scripts": {
    "validate:openai": "node scripts/validate-openai-package.js",
    "smoke:openai-dispatch": "node plugins/api-governance-openai/scripts/smoke-execute-openai-tool-call.mjs",
    "validate:copilot": "node scripts/validate-copilot-kit.js",
    "validate:cursor": "node scripts/validate-cursor-plugin.js",
    "validate:tools-wire": "node scripts/validate-tools-wire.mjs",
    "validate:tools-wire:scheduled": "node scripts/validate-tools-wire.mjs --require-live",
    "selftest:tools-wire-modes": "node scripts/selftest-tools-wire-modes.mjs",
    "test": "node --test test/*.test.js && npm run validate:openai && npm run validate:copilot && npm run validate:cursor && npm run validate:tools-wire && npm run validate:rule-provenance && npm run selftest:tools-wire-modes",
    "validate:rule-provenance": "node scripts/validate-rule-provenance.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
